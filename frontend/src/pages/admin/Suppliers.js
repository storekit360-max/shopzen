import React,{useEffect,useState} from 'react';import API from '../../utils/api';import toast from 'react-hot-toast';
const initial={name:'',websiteUrl:'https://ugreen.lk/',syncEnabled:false,syncIntervalMinutes:15,defaultInStockQuantity:2,safetyStock:1,maximumSellableStock:'',requestTimeoutMs:15000,browserFallbackEnabled:true,isActive:true};
export default function Suppliers(){const [rows,setRows]=useState([]),[form,setForm]=useState(initial),[busy,setBusy]=useState('');const load=()=>API.get('/suppliers').then(r=>setRows(r.data)).catch(()=>toast.error('Failed to load suppliers'));useEffect(load,[]);const save=async e=>{e.preventDefault();if(!form.name||!/^https?:\/\//i.test(form.websiteUrl))return toast.error('Enter a supplier name and valid HTTP(S) URL');if(Number(form.syncIntervalMinutes)<5)return toast.error('Sync interval must be at least 5 minutes');setBusy('save');try{await API.post('/suppliers',{...form,maximumSellableStock:form.maximumSellableStock===''?null:Number(form.maximumSellableStock)});toast.success('Supplier added');setForm(initial);load()}catch(e){toast.error(e.response?.data?.message||'Could not save supplier')}finally{setBusy('')}};const act=async(id,action)=>{setBusy(id+action);try{const r=await API.post(`/suppliers/${id}/${action}`);toast.success(action==='test-connection'?r.data.message||'Connection tested':`${action} complete`);load()}catch(e){toast.error(e.response?.data?.message||'Supplier operation failed')}finally{setBusy('')}};const remove=async id=>{if(!window.confirm('Disable this supplier and its mappings? Existing products will be preserved.'))return;await API.delete(`/suppliers/${id}`);load()};return <div className="p-4 sm:p-6 max-w-7xl mx-auto"><div className="mb-6"><h1 className="text-2xl font-bold text-gray-900">Suppliers</h1><p className="text-sm text-gray-500 mt-1">Safely synchronize supplier availability with ShopZen stock.</p></div><form onSubmit={save} className="bg-white rounded-2xl border border-gray-100 p-4 sm:p-6 mb-6"><h2 className="font-semibold mb-4">Add supplier</h2><div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-3"><input className="form-input" placeholder="Supplier name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input className="form-input" placeholder="Website URL" value={form.websiteUrl} onChange={e=>setForm({...form,websiteUrl:e.target.value})}/><input className="form-input" type="number" min="5" placeholder="Sync interval (minutes)" value={form.syncIntervalMinutes} onChange={e=>setForm({...form,syncIntervalMinutes:e.target.value})}/><input className="form-input" type="number" min="0" placeholder="Default in-stock quantity" value={form.defaultInStockQuantity} onChange={e=>setForm({...form,defaultInStockQuantity:e.target.value})}/><input className="form-input" type="number" min="0" placeholder="Safety stock" value={form.safetyStock} onChange={e=>setForm({...form,safetyStock:e.target.value})}/><input className="form-input" type="number" min="0" placeholder="Maximum sellable (optional)" value={form.maximumSellableStock} onChange={e=>setForm({...form,maximumSellableStock:e.target.value})}/><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.syncEnabled} onChange={e=>setForm({...form,syncEnabled:e.target.checked})}/> Enable automatic sync</label></div><button disabled={busy==='save'} className="btn-primary mt-4">{busy==='save'?'Saving…':'Add Supplier'}</button></form><div className="space-y-4">{rows.length===0?<div className="bg-white rounded-2xl p-8 text-center text-gray-500">No suppliers configured.</div>:rows.map(s=><div key={s._id} className="bg-white rounded-2xl border border-gray-100 p-4 sm:p-6"><div className="flex flex-col sm:flex-row sm:justify-between gap-3"><div><h2 className="font-semibold text-lg">{s.name}</h2><a className="text-sm text-blue-600 break-all" href={s.websiteUrl} target="_blank" rel="noreferrer">{s.websiteUrl}</a><div className="flex flex-wrap gap-2 mt-2"><span className="px-2 py-1 rounded-full text-xs bg-gray-100">{s.connectionStatus}</span><span className="px-2 py-1 rounded-full text-xs bg-gray-100">{s.platform}</span><span className="px-2 py-1 rounded-full text-xs bg-gray-100">{s.syncEnabled?'Auto sync on':'Auto sync off'}</span></div><p className="text-xs text-gray-500 mt-2">{s.connectionMessage||'Not tested'} · {s.discoveredProductCount||0} discovered · {s.mappedProductCount||0} mapped</p></div><div className="flex flex-wrap gap-2 sm:max-w-sm sm:justify-end"><button disabled={!!busy} onClick={()=>act(s._id,'test-connection')} className="btn-secondary text-xs">Test</button><button disabled={!!busy} onClick={()=>act(s._id,'discover-products')} className="btn-secondary text-xs">Discover</button><button disabled={!!busy} onClick={()=>act(s._id,'sync')} className="btn-primary text-xs">Sync now</button><button onClick={()=>remove(s._id)} className="text-xs px-3 py-2 rounded-lg bg-red-50 text-red-700">Disable</button></div></div></div>)}</div></div>}
